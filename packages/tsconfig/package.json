{
  "name": "@aggregator/tsconfig",
  "version": "0.0.0",
  "private": true,
  "files": [
    "tsconfig.base.json",
    "tsconfig.node.json",
    "tsconfig.nestjs.json"
  ],
  "scripts": {
    "build": "echo 'No build step required'",
    "test": "echo 'No tests'",
    "lint": "echo 'No lint'"
  }
}
